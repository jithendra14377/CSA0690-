#include<stdio.h>
int main()
{
	int n;
	printf("The number of rows to be printed : ");
	scanf("%d" , &n);
	for(int i = 0; i <= (n - 1); i++)
	{
		for(int j = 0; j <= i; j++)
		{
			printf("%d\t" , (j + 1));
		}
		printf("\n");
	}
}
