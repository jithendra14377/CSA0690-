#include<stdio.h>
#include<stdlib.h>
int fact(int n)
{
	int m;
	if(n == 0)
	{
		return 1;
	}
	else
	{
		return fact(n - 1) * n;
	}
}

int main()
{
	int n;
	printf("Find factorial of : ");
	scanf("%d" , &n);
	printf("Factorial of %d is : %d" , n , fact(n));
}
